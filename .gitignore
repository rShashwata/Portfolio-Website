# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
# environment
.env
.env.*
# os
.DS_Store
Thumbs.db
# claude code local settings (machine-specific permissions)
.claude/settings.local.json
# pdf cover tool: local python venv + the drop folder for source PDFs
# (PDFs live on R2, not in the repo — only the rendered covers get committed)
tools/.venv/
tools/pdf-inbox/
