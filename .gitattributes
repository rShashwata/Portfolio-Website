# Normalize line endings to LF for all text files so the repo is consistent
# across Windows checkouts and Cloudflare's Linux build environment.
* text=auto eol=lf

# Treat binary assets as binary (never touch their bytes).
*.webp binary
*.jpg  binary
*.jpeg binary
*.png  binary
*.svg  text eol=lf
