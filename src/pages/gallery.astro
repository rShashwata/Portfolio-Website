---
// Alternate homepage: identical to index.astro but the Work section is a
// modern card gallery instead of a list. Lives at /gallery so the list
// version at / stays as-is. To make this the main homepage, swap its
// contents with src/pages/index.astro.
import Layout from '../layouts/Layout.astro';
import Preloader from '../components/Preloader.astro';
import Nav from '../components/Nav.astro';
import Hero from '../components/Hero.astro';
import Marquee from '../components/Marquee.astro';
import WorkGallery from '../components/WorkGallery.astro';
import About from '../components/About.astro';
import Cv from '../components/Cv.astro';
import Contact from '../components/Contact.astro';
import { profile } from '../data/site.js';
---

<Layout>
  <Preloader />
  <Nav />
  <main>
    <Hero />
    <Marquee items={profile.keywords} accent />
    <WorkGallery />
    <Marquee items={['Design', 'Edit', 'Motion', 'Color', 'Direct', 'Repeat']} reverse />
    <About />
    <Cv />
    <Contact />
  </main>
</Layout>

<style is:global>
  /* Magnetic buttons share this inner-wrapper transform target. */
  .magnetic {
    display: inline-block;
    will-change: transform;
  }
  .magnetic__inner {
    display: inline-block;
    will-change: transform;
  }
</style>
